

/**
 * vue 报错
 * @param {*} monitor 
 * @param {*} err 
 * @param {*} vm 
 * @param {*} info 
 * @param {*} Vue 
 */
export function handleVueError(monitor, err, vm, info,  Vue) {
    const version = Vue?.version
}