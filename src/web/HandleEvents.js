import { HTTP_CODE } from '../conmmon/constant'


const HandleEvents = {
    /**
     * 处理xhr、fetch回调
     * @param {*} data 
     * @param {*} type 
     */
    handleHttp(data, type) {
        
    },
}

export { HandleEvents }